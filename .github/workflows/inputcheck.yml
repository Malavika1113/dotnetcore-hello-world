name: "the 10 input limit"
on:
  workflow_dispatch:
    inputs:
      PARAMETERS:
        description: "A JSON object with all needed parameters"
        required: true
jobs:
  myJob:
    name: "My job"
    runs-on: ubuntu-latest
    outputs:
      MY_INPUT1: ${{ steps.setup-inputs.outputs.MY_INPUT1 }}
      MY_INPUT2: ${{ steps.setup-inputs.outputs.MY_INPUT2 }}
    steps:
      - name: Setup inputs from json
        id: setup-inputs
        run: |
            echo '${{ inputs.PARAMETERS }}' | jq -r 'to_entries[] | "\(.key)=\(.value)"' | xargs -I '{}' echo "{}" >> "$GITHUB_OUTPUT"
            echo "------------------------"
            echo '${{ inputs.PARAMETERS }}' | jq -r 'to_entries[] | "\(.key)=\(.value)"' | xargs -I '{}' echo "{}"
            echo "___________________________________"
            echo ${{ needs.setup-inputs.outputs.MY_INPUT1 }}
#       - name: Use output
#         run: | 
#             echo ${{ needs.setup-inputs.outputs.MY_INPUT1 }}
#             echo ${{ needs.setup-inputs.outputs.MY_INPUT2 }}
